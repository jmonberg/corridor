require 'test_helper'

class VoteTest < ActiveSupport::TestCase
  def test_article_association
    assert_equal articles(:one), votes(:one).article
  end
  
  test "the truth" do
    assert true
  end
end
